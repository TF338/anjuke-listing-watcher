[pytest]
markers =
    integration: tests that hit the live Anjuke website
    slow: tests that take longer to run

# Test discovery
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = -v --tb=short
